/**
 *Use the ptr pointer and complete the following program to read and store inte- gers into arr, with the restriction to store an input number only if it is less than the last stored number. The value −1 should not be stored in 
 the array. If the user enters 0 or if there is no other free place, the insertion of numbers should terminate and the program should display how many numbers were stored in the array. The program should prompt the user to input 
 the numbers, as follows:

 Enter number_1:
 Enter number_2:
 * 
*/

int main(void)
{
    int *ptr, arr[100];
}