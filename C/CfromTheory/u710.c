/**
 * Write a program that reads integers and stores them in a 3×4 array. Then, the pro- gram should find the maximum value of each row and assign that value 
 * to all preceding elements in the same row.
*/