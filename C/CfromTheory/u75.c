/**
 * Write a program that reads 100 integers and stores them in an array. Then, the pro- gram should rotate the elements one place to the right. For 
 * example, if the array was of four elements: 1, -9, 5, 3, the rotated array would be: 3, 1, -9, 5.
*/