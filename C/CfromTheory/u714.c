/**
 * Write a program that reads integers and stores them in a 5×5 square matrix. Then, the program should read an integer within [1, 5] (e.g., x) and 
 * copy in a second array the elements of the first array, except the elements of the x-row and x-column.
*/