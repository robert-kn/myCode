/* Write a program that reads characters and stores them in an array of 100 places
with the restriction that no duplicated character is stored. If the user enters 'q' or the array
is full, the insertion of characters should terminate. */