/* Write the functions f() and g(), as follows: (image in file u112.png) */

/* Write a program that reads an integer (e.g., x) and uses f() and g() to display the result
of f(g(x)), with the restriction that g() must be called from inside f(). */