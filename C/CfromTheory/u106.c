/* Write a program that reads a string of less than 100 characters and stores it in an
array. Then, the program should reverse the stored string and display the new one. For
example, if the string code is stored, the program should reverse it and store in the array
edoc. Use just one array. */