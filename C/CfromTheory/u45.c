/**
 * Write a program that reads two integers, stores them into two variables, and uses the ^ operator 
 * and the equations (x^y)^x = y and (x^y)^y = x to swap their values. Use only two variables.
*/