/**
 * Write a program that assigns two negative values into two integer variables and uses 
 * those variables to display the corresponding positives.
*/

#include <stdio.h>

int main(void){
    int a = -2, b = -9;
    printf("a = %d, b = %d\n", a, b);
    return 0;
}