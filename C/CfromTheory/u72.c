/**
 * Write a program that reads double numbers continuously and stores in an array of 100 places those with a value more than 5. If the array becomes full or 
 * the user enters -1 the insertion of numbers should terminate. The program should display the minimum of the values stored in the array.
*/