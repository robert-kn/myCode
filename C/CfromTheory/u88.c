/**
 * Complete the following program by using the p1 pointer to read 100 integers, the p2 pointer to display the minimum of those with values less than -5, and the p3 pointer to display the maximum of those greater than 10. 
 * If no value less than -5 or greater than 10 is entered, the program should display an informative message.
*/

int main(void)
{
    int *p1, *p2, *p3, i, num, min, max;
}