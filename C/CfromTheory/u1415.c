/* In the binary search tree program, add a recursive function that takes as parameters a pointer to the 
root and a key value. If the key is found in the tree, the function should return a pointer to that node,
NULL otherwise.*/