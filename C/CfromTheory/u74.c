/**
 * Write a program that reads integers and stores them in an array of 100 places with the restriction that an input number is stored in the array only 
 * if it is less than the last stored. The program should terminate once the array becomes full.
*/