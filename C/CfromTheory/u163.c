/* The following macro calculates the minimum of two numbers. It is not correct,
though. Pass an expression with the & operator as an argument, to prove the error. Fix the
definition.
#define MIN(a, b) (a < b ? a : b) */