#include <stdint.h>
#include <stdio.h>
#include <inttypes.h>

int main(void)
{
    int32_t x;
    int64_t y;
    printf("x = %" PRId32 ", y = %" PRIu64 "\n", x ,y);
    printf("x = %d, y = %d", INT_MIN)
    return 0;
}