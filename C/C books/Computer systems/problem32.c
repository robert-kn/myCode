
// For each of the following lines of assembly language, determine the appropriate instruction suffix based on the 
// operands. (For example, mov can be rewritten as movb, movw, or movl.)

// 1 movl   %eax, (%esp)
// 2 movw   (%eax), %dx
// 3 movb   $0xFF, %bl
// 4 movb   (%esp,%edx,4), %dh
// 5 push1  $0xFF
// 6 movw   %dx, (%eax)
// 7 pop1   %edi


/* global edition. solutions in pg 361 */
// For each of the following lines of assembly language, determine the appropriate instruction suffix based on the 
// operands. (For example, mov can be rewritten as movb, movw, movl, or movq.)

// movl     %eax, (%rsp)
// movw     (%rax), %dx
// movb     $0xFF, %bl
// movb     (%rsp,%rdx,4), %dl
// movq     (%rdx), %rax
// movw     %dx, (%rax)





