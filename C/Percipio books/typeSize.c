#include <stdio.h>

void main(){
    int i;
    char c;
    float a;
    double b;
    system("clear");
    printf("size of int %zu, size of char %zu, size of float %zu, size of double %zu\n", sizeof(i), sizeof(c), sizeof(a), sizeof(b));
}